{
  "name": "@cos/shared",
  "version": "0.2.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "dev": "tsc --watch --project tsconfig.build.json",
    "type-check": "tsc --noEmit",
    "clean": "node ../../../scripts/rimraf-dist.mjs",
    "test": "echo \"@cos/shared exports TypeScript types only — no executable logic to test (Rule 35 exempt)\" && exit 0",
    "test:unit": "echo \"@cos/shared exports TypeScript types only — no executable logic to test (Rule 35 exempt)\" && exit 0",
    "test:cov": "echo \"@cos/shared exports TypeScript types only — no executable logic to test (Rule 35 exempt)\" && exit 0"
  },
  "dependencies": {
    "@cos/types": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.12.0",
    "typescript": "^6.0.3"
  }
}
