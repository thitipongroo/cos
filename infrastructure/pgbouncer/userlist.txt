; PgBouncer userlist — local dev only.
; Plaintext password (matches POSTGRES_PASSWORD default in docker-compose.yml) so PgBouncer
; can perform SCRAM-SHA-256 authentication to PostgreSQL 16 (which stores SCRAM verifiers).
; The edoburu/pgbouncer image otherwise auto-generates an md5 userlist, which cannot do
; SCRAM auth to the server ("wrong password type"). Production injects this via Vault/SM.
"cos" "cos_dev_password"
