# ArgoCD PostSync hook — wave 1 smoke test
# Spec §30.12: smoke tests (ArgoCD PostSync wave 1) → blocks E2E wave 2
# Mirrors scripts/deploy/smoke-test.sh.
#
# ⚠️ THIS HOOK DOES NOT FIRE (measured 2026-08-23). An ArgoCD PostSync hook only runs for manifests
# inside the synced Application's source path. Every Application points at infrastructure/helm/cos-*
# or the otel overlays; this file is in infrastructure/kubernetes/argocd/, which no Application syncs.
# Applied by hand it is a plain Job, not a hook.
#
# ⚠️ AND `namespace: cos` IS PRODUCTION. The line below used to claim this "runs against staging
# after every ArgoCD sync" — staging is `cos-staging`. Both are placement decisions that belong with
# the staging run that closes docs/runbooks/deployment.md, so nothing is moved here unilaterally.
#
# The auth step now goes to Keycloak's token endpoint with a NON-PRIVILEGED smoke user; it used to
# POST /api/v1/auth/login, which has never existed. See the header of scripts/deploy/smoke-test.sh —
# a privileged user cannot be used, because the realm denies TENANT_ADMIN and FINANCE on Direct
# Grant.
---
apiVersion: batch/v1
kind: Job
metadata:
  name: cos-smoke-test
  namespace: cos
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
    argocd.argoproj.io/sync-wave: '1'
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cos-smoke-test
        app.kubernetes.io/component: smoke-test
    spec:
      restartPolicy: Never
      containers:
        - name: smoke-test
          image: curlimages/curl:8.7.1
          command: ['/bin/sh', '-c']
          args:
            - |
              set -euo pipefail
              START=$(date +%s)

              fail() { echo "SMOKE FAIL: $1" >&2; exit 1; }

              echo "→ [1/3] Health check"
              STATUS=$(curl -sf -o /dev/null -w "%{http_code}" --max-time 10 "${BASE_URL}/api/v1/health/ready") \
                || fail "health endpoint unreachable"
              [ "$STATUS" = "200" ] || fail "health returned HTTP ${STATUS}"
              echo "  ✓ /api/v1/health/ready → 200"

              echo "→ [2/3] Auth check"
              # --data-urlencode, not -d: an E.164 username starts with '+', which -d sends literally
              # and the server decodes as a SPACE.
              AUTH=$(curl -sf --max-time 10 -X POST \
                "${KEYCLOAK_URL}/realms/${KEYCLOAK_REALM}/protocol/openid-connect/token" \
                --data-urlencode "grant_type=password" \
                --data-urlencode "client_id=${KEYCLOAK_CLIENT_ID}" \
                --data-urlencode "client_secret=${KEYCLOAK_CLIENT_SECRET}" \
                --data-urlencode "username=${SMOKE_USER}" \
                --data-urlencode "password=${SMOKE_PASSWORD}") \
                || fail "Keycloak refused the grant (is SMOKE_USER privileged?)"
              TOKEN=$(echo "$AUTH" | grep -o '"access_token":"[^"]*"' | cut -d'"' -f4)
              [ -n "$TOKEN" ] || fail "Keycloak did not return an access_token"
              echo "  ✓ Keycloak token endpoint → access_token obtained"

              echo "→ [3/3] Core read check"
              P_STATUS=$(curl -sf -o /dev/null -w "%{http_code}" --max-time 10 \
                "${BASE_URL}/api/v1/projects" \
                -H "Authorization: Bearer ${TOKEN}") \
                || fail "projects endpoint unreachable"
              [ "$P_STATUS" = "200" ] || fail "projects returned HTTP ${P_STATUS}"
              echo "  ✓ /api/v1/projects → 200"

              END=$(date +%s)
              ELAPSED=$((END - START))
              echo "→ Smoke passed in ${ELAPSED}s"
              [ $ELAPSED -lt 30 ] || fail "exceeded 30s limit (${ELAPSED}s)"
          env:
            - name: BASE_URL
              valueFrom:
                secretKeyRef:
                  name: cos-smoke-test-config
                  key: BASE_URL
            # SMOKE_USER must be NON-PRIVILEGED. E2E_EMAIL (a TENANT_ADMIN) used to be wired here and
            # cannot work: the realm denies TENANT_ADMIN and FINANCE on Direct Grant.
            - name: SMOKE_USER
              valueFrom:
                secretKeyRef: { name: cos-smoke-test-config, key: SMOKE_USER }
            - name: SMOKE_PASSWORD
              valueFrom:
                secretKeyRef: { name: cos-smoke-test-config, key: SMOKE_PASSWORD }
            - name: KEYCLOAK_URL
              valueFrom:
                secretKeyRef: { name: cos-smoke-test-config, key: KEYCLOAK_URL }
            - name: KEYCLOAK_REALM
              valueFrom:
                secretKeyRef: { name: cos-smoke-test-config, key: KEYCLOAK_REALM }
            - name: KEYCLOAK_CLIENT_ID
              valueFrom:
                secretKeyRef: { name: cos-smoke-test-config, key: KEYCLOAK_CLIENT_ID }
            - name: KEYCLOAK_CLIENT_SECRET
              valueFrom:
                secretKeyRef: { name: cos-smoke-test-config, key: KEYCLOAK_CLIENT_SECRET }
