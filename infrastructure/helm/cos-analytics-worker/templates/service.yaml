apiVersion: v1
kind: Service
metadata:
  name: {{ include "cos-analytics-worker.fullname" . }}
  labels: {{ include "cos-analytics-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector: {{ include "cos-analytics-worker.selectorLabels" . | nindent 4 }}
