apiVersion: v1
kind: Service
metadata:
  name: {{ include "cos-web.fullname" . }}
  labels: {{ include "cos-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{ include "cos-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
