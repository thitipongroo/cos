# Apache Airflow scheduler/worker env — Phase 23 training-pipeline DAGs (dag_*.py).
# SEPARATE env: airflow cannot co-install with feast (uvicorn conflict). airflow 3.2.2
# requires_python ">=3.10,!=3.15"; bumped 2.x→3.x per product-owner decision 2026-06-30.
-r requirements-base.txt
apache-airflow==3.2.2
