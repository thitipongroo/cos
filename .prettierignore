node_modules
dist
build
.next
coverage
.turbo
*.avsc
*.proto
pnpm-lock.yaml
# Helm templates contain Go template syntax ({{ }}) — not valid YAML
infrastructure/helm/**/templates/**
# Large legacy reference docs — same exclusion as .markdownlintignore, kept in step with it.
# These carry a pre-existing formatting backlog measured on 2026-08-24 (38 files under
# docs/specifications/, 13 under context/, plus context.md). They were excluded from the
# markdownlint gate for that reason but not from the Prettier gate, so format:check and
# markdownlint disagreed about what counts as legacy. See §35.13 ESC-37 in docs/architecture/test-design/escalation-register.md.
docs/specifications/
context/
context.md

# Local Claude Code session state — never committed (globally gitignored), not format-gated
.claude/
# Design-tool export (DESIGN.md + code.html + screen.png per screen) — reference for implementation
mockup/

# ── Generated output ────────────────────────────────────────────────────────
#
# All of the following are gitignored, so CI's fresh checkout never sees them and `format:check`
# there is unaffected. They are listed for the DEVELOPER running the same command locally: after an
# Android build and a Python test run, `pnpm format:check` reported 154 failures in generated files
# and 38 in real ones, which made the command useless for finding the 38 that matter.
#
# `format:check` passes `--ignore-path .prettierignore` explicitly, which REPLACES prettier 3's
# default of [.gitignore, .prettierignore] — so being gitignored is not enough on its own, and each
# generated path has to be repeated here.
#
# CMake/NDK output from `expo run:android` — compile_commands.json, .cmake/api replies, prefab config.
apps/mobile/android/**/.cxx/
# pytest's cache directory (README.md + nodeids) in every Python service.
**/.pytest_cache/
# Python virtualenvs in the services that have one — site-packages ships other projects'
# LICENSE.md and dist-info JSON, which this command would otherwise report as ours.
**/.venv/
**/venv/
# Pact contract files, regenerated by `pnpm test:contract`.
pacts/
# Next.js writes this on build and its own header says not to edit it.
apps/web/next-env.d.ts
# CocoaPods output from `pod install` in apps/mobile/ios. Eighty-eight of the ninety-four files
# this command reported on 2026-09-04 were here: every `.xcframework` ships a `.swiftmodule` JSON
# and a dSYM Relocations `.yml`, and `Local Podspecs/` is one JSON per pod. All of it is written
# by CocoaPods and gitignored at apps/mobile/ios/.gitignore:30.
apps/mobile/ios/Pods/
# Provider binaries and module cache downloaded by `terraform init` (2 of the 94; gitignored,
# .gitignore:130). CI's lint job runs format:check BEFORE its terraform init step, so this tree
# only ever exists locally — but locally is where the command has to stay usable.
**/.terraform/
# Playwright's per-run output — error-context.md and .last-run.json (4 of the 94; gitignored,
# .gitignore:108).
test-results/
