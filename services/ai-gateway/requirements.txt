fastapi==0.138.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
opentelemetry-sdk==1.43.0
opentelemetry-instrumentation-fastapi==0.64b0
# opentelemetry-instrumentation imports pkg_resources, which Python 3.12 venvs no longer bundle.
# Provide setuptools (<81, where pkg_resources still ships) so FastAPIInstrumentor imports.
setuptools<81
opentelemetry-exporter-otlp-proto-http==1.43.0
opentelemetry-instrumentation-httpx==0.64b0
httpx==0.28.1
python-dotenv==1.2.2
# Runtime deps imported by the report pipeline, cache, and digital-twin modules — these were
# missing from requirements (service never started). Lower-bound pins; pip resolves compatible.
asyncpg>=0.29
redis>=8.0
aiokafka>=0.11
jinja2>=3.1
