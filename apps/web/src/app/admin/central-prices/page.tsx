'use client';

import { CentralPriceRegister } from '../../../components/admin/CentralPriceRegister';

/** `/admin/central-prices` — ราคากลาง Central Price Register (R17). The screen is <CentralPriceRegister />. */
export default function AdminCentralPricesPage() {
  return <CentralPriceRegister />;
}
