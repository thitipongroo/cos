'use client';

import { ClusterInfrastructure } from '../../../components/admin/ClusterInfrastructure';

/** `/admin/cluster` — Cluster Infrastructure & Fleet Telemetry (R17). The screen is <ClusterInfrastructure />. */
export default function AdminClusterPage() {
  return <ClusterInfrastructure />;
}
