'use client';

import { DataMigrations } from '../../../components/admin/DataMigrations';

/** `/admin/migrations` — Data Migrations & Approval Gate (R17). The screen is <DataMigrations />. */
export default function AdminMigrationsPage() {
  return <DataMigrations />;
}
