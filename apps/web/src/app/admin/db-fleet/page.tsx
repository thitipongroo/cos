'use client';

import { DedicatedDbFleet } from '../../../components/admin/DedicatedDbFleet';

/** `/admin/db-fleet` — Dedicated DB Fleet Management (R17). The screen is <DedicatedDbFleet />. */
export default function AdminDbFleetPage() {
  return <DedicatedDbFleet />;
}
