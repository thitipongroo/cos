'use client';

import { GlobalAuditLog } from '../../../components/admin/GlobalAuditLog';

/** `/admin/audit` — Global Audit Log (R17). The screen is <GlobalAuditLog />. */
export default function AdminAuditPage() {
  return <GlobalAuditLog />;
}
