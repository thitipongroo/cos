'use client';

import { TenantListScreen } from '../../components/admin/TenantList';

/** `/admin` — the SYSTEM_ADMIN Tenant List (§20.4.1). The screen is <TenantListScreen /> (R13). */
export default function AdminTenantListPage() {
  return <TenantListScreen />;
}
