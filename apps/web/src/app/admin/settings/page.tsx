'use client';

import { SystemSettings } from '../../../components/admin/SystemSettings';

/** `/admin/settings` — System Settings (R17). The screen is <SystemSettings />. */
export default function AdminSettingsPage() {
  return <SystemSettings />;
}
