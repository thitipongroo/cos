# apps/mobile installs STANDALONE (excluded from the repo-root pnpm workspace; it has its own
# pnpm-workspace.yaml). Hoisting is configured there via `nodeLinker: hoisted` (pnpm 10/11 reads the
# linker setting from pnpm-workspace.yaml, NOT this .npmrc). Install with:  cd apps/mobile && pnpm install
