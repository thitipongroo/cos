<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
    <!-- Icon = the square brand mark (hexagon), so it fills the Android-12 splash icon slot instead
         of the wide wordmark being letterboxed tiny. Wordmark moves to the branding image below. -->
    <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen_logo</item>
    <!-- Branding image (API 31+) — the "CONSTRUCTION OS" wordmark, shown centred at the bottom. -->
    <item name="android:windowSplashScreenBrandingImage">@drawable/splashscreen_branding</item>
    <item name="postSplashScreenTheme">@style/AppTheme</item>
    <item name="android:windowSplashScreenBehavior">icon_preferred</item>
  </style>
</resources>